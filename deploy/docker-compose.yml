name: img-upload
services:
  postgres:
    image: postgres:16-alpine
    container_name: pf-img-db
    ports:
      - ${POSTGRES_PORT}:5432
    volumes:
      - pf-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_DB=${POSTGRES_DB}
    networks:
      - pf-net
  backend:
    depends_on:
      - postgres
    image: supat1400/preflight-backend:latest
    container_name: pf-img-backend
    
    ports:
      - ${PORT}:3000
    volumes:
      - pf-image:/app/dist/images
    networks:
      - pf-net
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_PORT=${POSTGRES_PORT}
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_APP_USER=${POSTGRES_APP_USER}
      - POSTGRES_APP_PASSWORD=${POSTGRES_APP_PASSWORD}
  frontend:
    depends_on:
      - backend
    image: supat1400/preflight-frontend:latest
    container_name: pf-img-frontend
    ports:
      - ${NGINX_PORT}:5899
    networks:
      - pf-net
    environment:
      - NGINX_PROXY=${NGINX_PROXY}
      - NGINX_PORT=${NGINX_PORT}
volumes:
  pf-data:
  pf-image:
networks:
  pf-net:




  